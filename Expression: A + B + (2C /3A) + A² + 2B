#include<stdio.h>

int main() {
    float A, B, C, result;

    printf("Enter A, B, C: ");
    scanf("%f %f %f", &A, &B, &C);

    result = A + B + (2 * C / (3 * A)) + (A * A) + (2 * B);

    printf("Result = %.2f", result);
    return 0;
}

OUTPUT:Enter A, B, C: 2 3 4
Result = 17.33
